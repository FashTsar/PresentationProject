# Customization options
# WebDriver - only WebDriver tests are running
# API - if there is an API test, then it runs, otherwise the WebDriver test runs.

# ----- for Cart -----
priorityRunningTestForCart=WebDriver